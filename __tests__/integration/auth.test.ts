describe('authenticate', () => {
    it('should return jwt token when valid credentials', () => {
        const x = 1
        const y = 3

        const sum = x + y
        expect(sum).toBe(4)

    })
})

describe('register', () => {

})